import React from 'react';
import { Box, Typography } from '@mui/material';

const CustomerPayers = () => {
    return (
        <Box>
            <Typography variant="h6" color="text.primary">Reporte de Clientes Pagadores</Typography>
            <Typography color="text.primary">Contenido del reporte de clientes pagadores...</Typography>
        </Box>
    );
};

export default CustomerPayers;